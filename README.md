# Backend project for a simple dashboard

### Prerequisites

Make sure you have the following software installed before running the app:

- [Docker](https://www.docker.com/)

to run the project

```
docker-compose up -d
```

now you can used it at port 8000
